# XClarity ETL — Python dependencies
# NiFi API client
nipyapi==0.19.1

# Redis client (for exchange rate pre-load and SCD2 lookup)
redis==5.0.1

# PostgreSQL driver (psycopg2 binary — no compile needed in Docker)
psycopg2-binary==2.9.9

# HTTP / REST utilities
requests==2.31.0
urllib3==2.1.0

# Data processing utilities (used by regression harness and validation)
pandas==2.1.4
pyarrow==14.0.2

# Avro schema support
fastavro==1.9.2

# Logging & config helpers
python-dotenv==1.0.0
