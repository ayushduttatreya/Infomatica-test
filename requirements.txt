# XClarity ETL – Informatica to Apache NiFi Migration
# Python dependencies

# NiFi Python API client
nipyapi==0.19.1

# Redis client (for exchange rate pre-loading and SCD2 cache ops)
redis==5.0.1

# PySpark (for data validation harness and regression diff)
pyspark==3.5.1

# PostgreSQL adapter (for lookup table seeding)
psycopg2-binary==2.9.9

# HTTP client (NiFi REST API calls where nipyapi doesn't cover)
requests==2.31.0
urllib3==2.1.0

# Data manipulation (regression diff harness)
pandas==2.1.4
numpy==1.26.3

# CSV/Avro utilities
fastavro==1.9.3
python-dateutil==2.8.2

# Logging/config
python-dotenv==1.0.0

# Testing
pytest==7.4.4
pytest-cov==4.1.0
